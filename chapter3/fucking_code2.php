<?php

if ($firstEnqueteCampaign != 0 || $womanUserType != 0 || $isInviteTargetUser === true) {
    // なんかする
    if ($firstEnqueteCampaign === 1 || $womanUserType === 1) {
        // なんかする
    }
}

if ($firstEnqueteCampaign != 1 || $womanUserType != 1 || $isInviteTargetUser === true) {
    // なんかする
    if ($firstEnqueteCampaign === 2 || $womanUserType === 2) {
        // なんかする
    }
}
